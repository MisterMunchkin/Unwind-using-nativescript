android {
            defaultConfig {
                applicationId = "org.nativescript.Unwind"

                if (false) {
                    minSdkVersion = false
                }

                if (false) {
                    targetSdkVersion = false
                }
            }
        }